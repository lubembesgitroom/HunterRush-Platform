{
  "name": "@hunterrush/gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",

  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },

  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@hunterrush/game-engine": "workspace:*",
    "fastify": "^5.10.0",
    "socket.io": "^4.8.3"
  },

  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.23.0",
    "typescript": "^5.9.2",
    "rimraf": "^6.0.1"
  }
}